# 贡献指南

感谢你参与改进 AIGC 文本检测评测框架。本项目公开仓库只接收可复用、已脱敏且与评测框架定位一致的修改。

## 开始之前

提交 Issue 或 Pull Request 前，请确认内容不包含：

- 真实用户文本、问题池或逐条预测结果；
- 接口地址、IP、端口、账号、token、cookie 或其他凭证；
- 内部模型名称、服务器信息、日志、目录和部署拓扑；
- 未经授权的数据、指标、截图或报告。

安全问题或疑似敏感信息不应粘贴到公开 Issue 中。

## 开发环境

- Python 3.10 或更高版本；
- 项目运行时仅使用 Python 标准库；
- CSV 文件使用 UTF-8 或 UTF-8 BOM 编码。

克隆仓库后无需安装运行时依赖，可以直接执行测试：

```powershell
python -m unittest discover -s tests -v
```

## 提交前检查

1. 运行完整测试，确保全部通过；
2. 检查新增测试是否覆盖正常路径、缺失字段和异常输入；
3. 检查命令行参数与现有调用保持兼容；
4. 检查原有 JSON 字段未被删除或改名；
5. 检查示例和文档不包含真实或可反向定位的信息；
6. 使用 `git diff --check` 检查空白和格式问题。

## 代码风格

- 使用 Python 3.10+ 类型注解；
- 为公开函数和复杂逻辑添加简洁 docstring；
- 优先复用现有模块和指标计算函数；
- 保持函数职责单一，错误信息清晰且不泄露原始文本；
- 默认只使用 Python 标准库，不引入新的运行时依赖；
- 如确需增加依赖，应先在 Issue 中说明用途、维护成本和替代方案。

仓库在 `pyproject.toml` 中提供基础 Ruff 和 mypy 配置，但当前 CI 的必要门禁是标准库单元测试。

## 功能兼容原则

- 新增分析功能必须默认关闭，并通过显式命令行参数启用；
- 不改变 AI 为正类、人工文本为负类的定义；
- 不破坏 `validate.py`、`evaluate.py` 和 `compare.py` 的现有调用方式；
- 新增 JSON 字段只能追加，不能删除或重命名已有字段；
- 可选字段缺失时应给出明确警告，不影响基础评估；
- 新功能必须补充单元测试，跨脚本流程应补充集成测试。

## 提交信息

提交信息使用简洁的祈使句，描述修改目的。建议格式：

```text
Add confidence bucket validation
Fix duplicate ID error handling
Document generator grouping
```

每个提交应聚焦一个完整主题，避免混入无关格式调整或生成文件。

## Issue 与 Pull Request

- Bug 请使用 Bug Report 模板，并提供最小化、已脱敏的复现输入；
- 功能建议请说明使用场景、预期收益和替代方案；
- Pull Request 应说明改动内容、兼容性影响和验证命令；
- 提交 PR 前应确保 GitHub Actions 在所有支持的 Python 版本上通过。
